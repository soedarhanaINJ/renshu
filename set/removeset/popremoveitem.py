setini = {'Cirebon', 'Bandung', 'Subang'} #item on set
x = setini.pop() #pop will remove item randomly, u cant be sure what item want remove

print(x) #the return of item is item has been removed with .pop()
print(setini)

#Note: Sets are unordered, so when using the pop() method, you do not know which item that gets removed.